![[Pasted image 20250604171911.png]]
![[Pasted image 20250604172032.png]]


### 1.Language Models
![[Pasted image 20250604211213.png]]

![[Pasted image 20250606214326.png]]

### Some requirements.txt
```
# LangChain Core
langchain
langchain-core
langchain-community

# OpenAI Integration
langchain-openai
openai

# Anthropic Integration
langchain-anthropic

# Google Gemini (PaLM) Integration
langchain-google-genai
google-generativeai


# Hugging Face Integration
langchain-huggingface
transformers
huggingface-hub


# Environment Variable Management
python-dotenv

# Machine Learning Utilities
numpy
scikit-learn
```


### Closed source model
#### Using openai LLM api

```python
from langchain_openai import OpenAI
from dotenv import load_dotenv

load_dotenv()

llm = OpenAI(model='gpt-3.5-turbo-instruct')
result = llm.invoke("What is the capital of India")
print(result)
```

#### Using Openai chatmodel
```python
from langchain_openai import ChatOpenAI
from dotenv import load_dotenv

load_dotenv()

model = ChatOpenAI(model='gpt-4', temperature=1.5, max_completion_tokens=10)

result = model.invoke("Write a 5 line poem on cricket")

print(result.content)
```
![[Pasted image 20250606233407.png]]


### Open source Model

![[Pasted image 20250607130636.png]]
![[Pasted image 20250607130901.png]]

==HuggingFace - The largest repository of open source LLMS==

There is three way to run open source model
1. use Hugging face inference API
2. Host your LLMs in hosting service
3. or run locally

### Disadvantage of using opensource model
![[Pasted image 20250607133133.png]]


### Example code for huggingface api call

```python 
from langchain_huggingface import HuggingFaceEndpoint
from dotenv import load_dotenv

load_dotenv()

llm = HuggingFaceEndpoint(
	repo_id="HuggingFaceH4/zephyr-7b-beta",
	task="text-generation",
	provider="hf-inference"
)

result = llm.invoke("What is the capital of India?")
print(result)
```


### Example code for Huggingface local model
```python
from langchain_huggingface import ChatHuggingFace, HuggingFacePipeline

llm = HuggingFacePipeline.from_model_id(
		model_id='TinyLlama/TinyLlama-1.1B-Chat-v1.0',
		task='text-generation',
		pipeline_kwargs=dict(
			temperature=0.5,
			max_new_tokens=100
		)
	)

model = ChatHuggingFace(llm=llm)
result = model.invoke("What is the capital of India")

print(result.content)
```